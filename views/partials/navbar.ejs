<!-- views/partials/navbar.ejs -->
<nav class="navbar bg-base-100 px-4 py-2">
    <div class="flex-1">
      <!-- Logo / Brand Name -->
      <a class="btn btn-ghost normal-case text-xl" href="/">My Website</a>
    </div>
  
    <!-- Desktop Menu -->
    <div class="hidden md:flex gap-4">
      <a class="btn btn-ghost normal-case <%= (page === '/' ? 'active' : '') %>" href="/">Home</a>
      <a class="btn btn-ghost normal-case <%= (page === '/solutions/projects' ? 'active' : '') %>" href="/solutions/projects">View Projects</a>
      <a class="btn btn-ghost normal-case <%= (page === '/about' ? 'active' : '') %>" href="/about">About</a>
    </div>
  
    <!-- Mobile Menu Button -->
    <div class="md:hidden">
      <button onclick="toggleMenu()" class="btn btn-square btn-ghost">
        ☰
      </button>
    </div>
  
    <!-- Mobile Menu (Hidden by Default) -->
    <div id="mobileMenu" class="hidden absolute top-16 right-4 bg-base-100 shadow-md rounded-lg w-48 flex flex-col">
      <a class="btn btn-ghost <%= (page === '/' ? 'active' : '') %>" href="/">Home</a>
      <a class="btn btn-ghost <%= (page === '/solutions/projects' ? 'active' : '') %>" href="/solutions/projects">View Projects</a>
      <a class="btn btn-ghost <%= (page === '/about' ? 'active' : '') %>" href="/about">About</a>
    </div>
  </nav>
  
  <!-- JavaScript to Handle Mobile Menu -->
  <script>
    function toggleMenu() {
      document.getElementById("mobileMenu").classList.toggle("hidden");
    }
  </script>
  